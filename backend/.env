# Variáveis de Ambiente para o Backend (Node.js/Express)

# Configuração do Servidor
PORT=3001
NODE_ENV=development
SECRET_KEY=sua_chave_secreta_aqui # Mude para uma chave forte em produção

# Configuração do Frontend (para CORS)
# O frontend estará rodando na porta 3000 (padrão do Vite)
FRONTEND_URL=http://localhost:3000

# Configuração do Banco de Dados (PostgreSQL)
# ATENÇÃO: Você precisará ter um servidor PostgreSQL rodando localmente
DB_HOST=localhost
DB_PORT=5432
DB_NAME=genesix_db
DB_USER=postgres
DB_PASSWORD=postgres

# Configurações de OAuth (Exemplo, se necessário)
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
